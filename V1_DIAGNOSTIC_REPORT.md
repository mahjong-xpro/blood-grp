# Mahjong AI V1 性能诊断报告 (原始项目)

## 执行摘要
V1 系统（原始 `mortal` 项目）在 **700k steps** 左右达到了性能峰值，随后由于计划中的 "Phase 6" 滚动发布导致了系统不稳定。我们在 TensorBoard 中看到的排名 (Ranking) 和 PT 指标的“倒退”，实际上是训练动力学发生剧烈变化的直接结果，特别是 Target Network、Oracle 蒸馏和深层 Bellman 更新之间产生了梯度冲突。

## 技术发现

### 1. “死亡螺旋”与 Phase 6 回滚
*   **根因**: 在 706k-715k steps 期间，系统同时启用了 **Target Network (TN)**、**Oracle Guiding** 和 **TD(λ=0.95)** 深层更新。
*   **不稳定性**: 从纯蒙特卡洛 (MC) 到使用移动目标 (TN) 的 1-step TD 的突然转变，引发了巨大的梯度尖峰和性能崩溃。
*   **当前状态**: 配置文件显示已回滚至“Phase 5 稳态”，但模型仍保留了那次崩溃后的残余方差。

### 2. 定缺：监督学习与强化学习 (RL) 的不匹配
*   **准确率的假象**: TensorBoard 显示 `ding_que/aux_match_rate` 和 `dqn_match_rate` 始终保持在 **~99%**。
*   **现实情况**: 虽然模型能够准确*预测*最佳花色（监督任务），但最终的 RL 价值函数并不总能将其转化为胜率最优的动作。
*   **约束**: 原始项目使用独立的 `AuxNet` 处理定缺；虽然它共享 `Brain` 编码器，但一旦辅助任务收敛到 99% 的准确率底线，RL 头的梯度可能就无法得到足够的强化。

### 3. 指标背离 (平均排名 vs 平均 PT)
*   **现象**: 平均排名退化至 ~2.5（随机水平基准），而平均 PT 下降。
*   **原因**: 随着奖励塑形（和牌奖、放铳惩）在 Phase 4 被淘汰，模型从“追求安全”转变为“追求分差最大化”。这通常会导致更高方差的打法，在“平均排名”上看起来像是退步，但从长期的分数最大化角度来看可能更健康。

### 4. 代码中发现的 ResNet 瓶颈
*   **MODEL-06 修复**: 我发现原始 V1 ResNet 存在严重的瓶颈（在 Flatten 之前将 192 通道压缩到了 32）。这很可能限制了 Oracle 向 Student 传授复杂战略细微差别的能力。

## 诊断建议
*   **Baseline 池过拟合**: V1 依赖标准自博弈。如果 Baseline 最近没有更新，模型可能会针对其自身的一个特定旧版本进行过拟合。
*   **容量升级**: 在 `model.py` 中发现的架构限制（192ch，30 blocks）是 1M steps 出现性能平台期的主要原因。
*   **V2 方向**: V2 系统重点关注的 **256x20 Bottleneck ResNet** 和 **状态感知定缺特征** 正是突破 V1 2.5 排名平台期所必需的。

---
*本报告基于对 `mortal/` 源代码和 localhost:6006 TensorBoard 指标的深度分析生成。*
