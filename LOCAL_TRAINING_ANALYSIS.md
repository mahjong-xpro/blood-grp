# 本地训练指标深度分析报告 (Localhost:6006)

> **当前阶段**: **Phase 2: 收敛/策略成型期 (Hard Mode) -> 延长战**
> **最新状态**: **Step 245,000 (严重回调 / 验证毒性)**

---

## 侧写分析：为什么模型会选择"地狱单骑" (Hell Wait)?

**用户案例**: 模型选择听 5 万，但场上已现 3 张，仅剩 1 张（绝张/地狱等待）。为什么不求更好的牌型？

### 1. 机制核查 (Sanity Check)
我们检查了 Rust 代码 (`libblood/src/algo/sp/state.rs`)：
*   **计算正确**: SP 计算器正确使用了 `tiles_seen`。它明确知道 **5 万只剩 1 张**。
*   **概率正确**: 输入给模型的 `win_prob` 特征会非常低 (约等于 1/N)。
*   **结论**: 模型**不是瞎子**。它是在"明知胜率极低"的情况下，**主动选择**了这个听牌。

### 2. 动机分析 (Why?)
既然不是 Bug，那就是 **策略抉择**。模型选择"绝张听牌"通常有以下理由：

#### A. 查叫压力 (Cha Jiao / Tenpai Status)
*   **血战核心规则**: 牌墙摸完时，未听牌者(No-Tenpai)要赔付给听牌者(Tenpai)。
*   **得失**: 哪怕听一张"死牌" (0 张)，只要保持听牌状态，就能避免赔付，甚至还能收钱。
*   **判断**: 如果当前是**中盘或尾盘**，或者有人已经定缺快做完了，模型会极度优先保证 **"先听了再说"**。

#### B. 防守避险 (Defense)
*   回退到一向听 (1-Shanten) 需要打出什么牌？
*   如果为了追求好型需要打出一张 **高危牌 (生张/Dora)**，模型会选择 **"安全地听烂牌"**。
*   指标 `avg_rank` 持续走低 (2.46) 说明模型非常注重防守。

### 3. 优化点 (Optimization)
如果这发生在**早盘 (Early Game)** 且 **无安危威胁**，那就是模型**太怂了 (Over-conservative)**。
*   **可能原因**: 训练中的惩罚 (Deal-in/No-Tenpai) 权重过大，导致模型不敢做大牌，只求苟活。
*   **建议**: 观察 Step 245k 回调后的表现。如果模型恢复"理智"，说明之前的 `ding_que_weight=5.0` 可能也干扰了这里的价值判断（强行定缺导致牌型做烂）。

---

## 1. 战况更新 (Step 280k: 高位企稳)
 
**状态优秀 (Consolidation)。**
模型在突破 3.10 后出现了健康的回调，现在正在 **3.05 - 3.07** 这一更高的新平台上企稳。

| 关键节点 | 步数 | 平均得点 (`avg_pt`) | 平均排名 (`avg_rank`) | 定缺匹配率 | 状态 |
|:---|:---:|:---:|:---:|:---:|:---|
| 企稳 | 255k | 3.08 | 2.46 | 80.8% | 稳定 |
| 横盘 | 265k | 3.03 | 2.48 | 80.6% | 蓄力 |
| 突破 | 270k | 3.10 | 2.45 | 80.9% | 冲高 |
| 回调 | 275k | 3.05 | 2.47 | 80.8% | 正常 |
## 1. 战况更新 (Step 287k: 心跳监测)
 
**状态活跃 (Active Heartbeat)。**
在等待 Step 290k 大考的同时，我们捕获到了 Step 287k 的中间态数据。

| 关键节点 | 步数 | 平均得点 (`avg_pt`) | 平均排名 (`avg_rank`) | 定缺匹配率 | 状态 |
|:---|:---:|:---:|:---:|:---:|:---|
## 1. 战况更新 (Step 300k: 最终验收)
 
**Phase 2 完美收官 (Mission Accomplished).**
Step 300k 的最终数据已经出炉，所有指标均满足 Phase 3 启动标准。

| 关键节点 | 步数 | 平均得点 (`avg_pt`) | 平均排名 (`avg_rank`) | 定缺匹配率 | 状态 |
|:---|:---:|:---:|:---:|:---:|:---|
| 横盘 | 285k | 3.07 | 2.46 | 80.5% | 延续 |
| 爆发 | 290k | 3.17 | 2.41 | 80.5% | 冲高 |
| 铁底 | 295k | 3.07 | 2.47 | 80.8% | 确认 |
| **最终** | **300k** | **3.09** | **2.46** | **80.8%** | **✅ 通关** |

## 2. Phase 3 启动授权 (Authorization)

**判定结果: GO (允许启动)**

### 验收清单 (Checklist)
1.  [x] **平均得点**: 3.09 > **3.05** (Pass)
2.  [x] **平均排名**: 2.46 < **2.48** (Pass)
3.  [x] **定缺稳定**: 80.8% (Pass)
4.  [x] **定缺通关**: `ce_loss` < 0.02 (Solved)

### 执行指令 (Action Item)
请立即执行以下操作进入 Phase 3：
1.  打开 `mortal/config.toml`。
2.  修改 **`boltzmann_epsilon`** 从 `0.15` 为 **`0.05`**。
3.  修改 **`boltzmann_temp`** 从 `0.2` 为 **`0.1`**。
4.  重启训练服务。
5.  **不要更新 `baseline.pth`** (等待系统自动触发)。

## 3. 面向未来 (Future Outlook)

**当前位置**: 300k / 300k (Phase 2 完成)

**Q: 什么时候启动 Phase 3 (精调期)？**
**A: 严格等到 Step 300,000 且满足稳定性指标。**

### 启动判定标准 (Go/No-Go Criteria)
在 Step 300k 时，必须同时满足：
1.  **平均得点 (`avg_pt`)**: 稳定在 **> 3.05** (近 20k 步均值)。
2.  **平均排名 (`avg_rank`)**: 稳定在 **< 2.48**。
3.  **定缺稳定性**: `match_rate` 波动 < 0.5%。

### Phase 3 配置预告 (Hardcore Mode)
如果满足上述条件，我们将修改 `config.toml` 进入"收官之战"：
*   **探索率 (Epsilon)**: `0.15` -> **`0.05`** (几乎不随机乱打，逼迫模型输出最优解)。
*   **温度 (Temp)**: `0.2` -> **`0.1`** (分布极度尖锐化，只信第一直觉)。
*   **目标**: 这是一个"提纯"过程。如果模型本身有缺陷，这个阶段会放大缺陷（崩盘）；如果模型地基打得好，这个阶段会把胜率再推高 1-2%。

**现在的任务**: **Hold & Wait**。不要在黎明前乱动。

## 4. 战略抉择：乘胜追击还是按部就班？(Strategic Decision)

**User Q: 这样爆发是不是还有上升空间？是要准备三阶段，还是继续等突破？**

**A: 坚持原计划，Step 300k 准时启动 Phase 3。**

### 深度逻辑推演
1.  **Exploration vs Exploitation (探索与利用)**
    *   现状 (Phase 2): Epsilon = 0.15。意味着每 100 张牌，AI 会**随机乱打** 15 张。
    *   成绩: 在 15% 乱打的情况下，它依然拿到了 3.17 的高分。
    *   推论: 如果把这 15% 的乱打去掉（Phase 3 降至 0.05），分数**必然**会进一步上升。

2.  **3.20 的阈值博弈**
    *   目标: 只要超过 3.20，对手就会变强 (Baseline Update)。
    *   现状: 3.17 (差距 0.03)。
    *   战术: Phase 3 的"提纯"操作（降低随机性）极大概率会直接把分数推过 3.20，从而**自动触发**对手更新。这比在 Phase 2 苦苦等待随机突破要稳健得多。

**结论 (Verdict):**
现在的爆发不是"等待"的理由，而是**启动 Phase 3 的底气**。Phase 3 才是把这部分潜在上升空间"变现"的最佳手段。

## 5. 对手更新策略 (Opponent Update Strategy)

**Q: 什么时候更新 baseline 对手？**
**A: 严格由 config.toml 的参数控制，不要手动干预。**

### 自动触发机制 (Automatic Trigger)
*   **配置**: `mortal/config.toml` 中设置了 `baseline_update_threshold = 3.2`。
*   **逻辑**: 训练脚本 (`train.py`) 会定期检查，当 Challenger 的 `avg_pt` 超过 **3.2** 时，系统会**自动**将当前的 `mortal.pth` 复制为 `baseline.pth`。
*   **现状**:
    *   Target: **3.20**
    *   Current: **3.03**
    *   Gap: **0.17**

**结论**: 手动更新是没有必要的，甚至是有害的（过早更新会让对手变弱，或者导致自我训练退化）。请耐心等待系统自动触发。

## 5. 深度健康诊断 (Deep Health Diagnosis)

既然你请求了"深度分析"，我们抛开表面数据，看透模型内部的健康状态：

### A. 算力释放 (Capacity Release)
*   **现象**: `ce_loss` (定缺损失) 已经降至 **0.02** 的极低水平，且长期横盘。
*   **意义**: 这是一个关键信号。意味着"定缺"这个辅助任务对模型来说已经**"通关" (Solved)**。
*   **影响**: 模型不再需要消耗大量的梯度更新来学习"缺哪门"。ResNet-50 的庞大算力现在几乎 **100% 释放** 给了核心打牌逻辑（牌效、防守、做牌）。这也是为什么 Step 250k 后 `avg_pt` 会突然爆发的原因。

### B. 损失平衡 (Loss Equilibrium)
*   **DQN Loss**: 稳定在 0.16-0.17 区间。没有发散（爆炸），也没有坍缩（归零）。
*   **解读**: 对于强化学习，Loss 不归零是**好事**。这意味着 Value Function 始终在捕捉新的 TD Error，还在不断地"微调"对局势的判断。如果 Loss 掉到 0，说明模型过拟合了；如果飙升，说明不收敛。现在的状态非常**健康**。

### C. 速度预测 (Velocity Projection)
按照目前的爬升速度 (Slope):
*   **245k -> 255k**: +0.18 pt (强力反弹)
*   **255k -> 260k**: -0.05 pt (回调)
*   **预测**: 下一个阻力位在 **3.10**。
*   **时间点**: 根据由于"边际递减效应"，越往上越难。预计到达 **3.20 (对手更新阈值)** 可能需要等到 **Step 280k - 290k**。这与我们的 **Step 300k Phase 3 计划** 完美重合。

**一句话总结**: 现在的回调是蓄力，模型正在把"定缺已通关"的红利转化为"打牌更细腻"的胜率。**Trust the process.**

## 6. 阈值合理性评估 (Threshold Assessment)

**User Q: 3.20 这个阈值是否合理？**

**A: 非常合理 (Very Reasonable)。理由如下：**

### 1. 数据证明 (Data Validity)
*   **现状**: Step 290k 已经达到了 **3.17**。
*   **差距**: 仅差 **0.03**。
*   **含义**: 一个在 "困难模式" (Epsilon=0.15) 下能打出 3.17 的模型，在 "正常模式" (Phase 3 Epsilon=0.05) 下几乎百分之百能超过 3.20。如果现在把阈值降到 3.15，等于我们 "手动修改了考试及格线"，而不是让优等生自己考过去。

### 2. 统治力要求 (Domination Requirement)
*   **Avg Pt 3.20** 代表什么？
    *   在 1v3 的对局中，主角平均每局净胜 3.2 分。这不仅是 "赢"，而是 **"碾压" (Domination)**。
    *   只有达到这种统治力，我们才应该更新对手 (Baseline)。如果只赢一点点 (比如 1.0) 就更新对手，模型还没站稳脚跟就遇到了更强的敌人，容易导致训练崩溃 (Collapse)。

### 3. 时间窗口 (Timing)
*   现在的 3.20 正好配合 Step 300k 的 Phase 3。
*   **剧本**: Step 300k 降噪 -> 分数自然突破 3.20 -> 自动触发对手更新 -> 完美衔接下一阶段。

**结论**: 3.20 是一个经过验证的、极具前瞻性的 "黄金阈值"。不要动它。

## 7. 进入三阶段 (Enter Phase 3) Protocol

**User Q: 进入3阶段, 我是否需要更新对手？**

**A: 不需要 (NO). 请严格遵守以下操作流程:**

### Phase 3 标准操作流程 (SOP)
1.  **动作**: 仅修改 `mortal/config.toml` 中的两个参数：
    *   `boltzmann_epsilon`: `0.15` -> **`0.05`**
    *   `boltzmann_temp`: `0.2` -> **`0.1`**
2.  **重启**: 重启训练 (`train.py`) 以应用新配置。
3.  **禁止**: **绝对不要手动更新 `baseline.pth`**。

### 为什么不要手动更新对手？
*   **机制**: 现有的 `baseline.pth` (对手) 是一个完美的 "磨刀石"。
*   **预测**: Phase 3 启动后，由于 Epsilon 降低 (AI 变聪明了)，`avg_pt` 会自然上涨。
*   **结果**: 当 `avg_pt` 自然突破 **3.20** 时，系统会 **自动 (Automatically)** 把当前最强的模型覆盖为新的 `baseline.pth`。
*   **风险**: 如果你现在手动更新，等于让一个 "还没完全证明自己" 的模型去当教官，这会导致训练效率下降。

**一句话**: 你只管改配置，剩下的交给系统。

## 8. Phase 3 核心看点 (Watchlist)

**配置已完美，无需其他修改 (Config is Perfect).**
进入 Phase 3 后，我们不再关注"做什么"，而是关注"发生了什么"。

请重点监控以下三个指标的**质变**：

### A. 3.20 的突破 (The Breakout)
*   **预期**: 在 Epsilon 降至 0.05 后，`avg_pt` 应该会以此前从未有过的速度爬升。
*   **信号**: 一旦突破 **3.20**，请立即检查终端日志，确认 **"Baseline Updated"** 消息出现。这是 Phase 3 的第一个里程碑。

### B. Loss 的进一步压缩 (The Squeeze)
*   **DQ Loss**: 在低随机环境下，Q 值估计会更加精准。观察 `loss/dqn_loss` 是否会从 0.16 进一步下探到 0.14-0.15 区间。
*   **意义**: 如果 Loss 下降，说明模型正在"抠细节"，把以前因为随机干扰而看不清的微小失误都修正了。

### C. 定缺的抗压测试 (Stress Test)
*   **风险**: 策略分布的剧变（从随机变为确定）有时会冲击定缺网络。
*   **红线**: 密切关注 `loss/ding_que_ce_loss`。如果不反弹（保持 < 0.02），说明模型极其强健；如果飙升 > 0.05，我们才需要干预。

**目前状态**: **静默观察 (Silent Observation).** 让子弹飞一会儿。
