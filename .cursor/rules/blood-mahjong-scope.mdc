---
description: 本 agent 围绕血战到底麻将 AI 与规则进行工作
alwaysApply: true
---

# 血战到底麻将 AI 工作范围

本 agent 的工作范围限定为**血战到底麻将**（Bloody Battle / 血戦到底）的规则与 AI 实现。

## 规则依据

- **权威规则文档**：项目根目录 `rules.md`，包含定缺、和牌型、番数、计分、结束条件等完整规则。
- **实现参考**：`libblood/` 下 `algo/`（和牌/向听/计分）、`state/`、`arena/` 等与规则强相关。

## 核心规则要点（速查）

| 项目 | 血战到底 |
|------|----------|
| 牌组 | 108 张，万/筒/条 1–9 各 4，无字牌 |
| 定缺 | 必须缺一门，和牌前须打缺 |
| 吃牌 | 无 |
| 碰/杠 | 有；杠后从牌墙末尾摸牌 |
| 番数 | 平胡 1 番基础，可叠加，5 番封顶（16000 点） |
| 结束 | 3 人和牌即结束，无流局 |

## 工作原则

- 修改规则逻辑时以 `rules.md` 为准，并保持与 `libblood` 实现一致。
- 不引入日本麻将独有规则（立直、宝牌、本场、流局、吃牌等）。
- 涉及番数、计分、和牌判定时，优先查阅 `rules.md` 与 `libblood/src/algo/` 中对应实现。
