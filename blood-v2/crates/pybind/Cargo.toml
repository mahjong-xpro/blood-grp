[package]
name = "blood_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "blood_engine"
crate-type = ["cdylib"]

[dependencies]
engine = { path = "../engine" }
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"
fastrand = "2"
